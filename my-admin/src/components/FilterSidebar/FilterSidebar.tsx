import { Card } from "@mui/material";

const FilterSidebar = () => {
  return (
    <Card sx={{ order: -1, mr: 2, mt: 9, width: 200 }}>
      <h3>Filters</h3>
    </Card>
  );
};

export default FilterSidebar;
